import NavBar from '../../components/NavBar/NavBar';

const Settings = () => {
    return (
        <section>
            <NavBar></NavBar>
            <h1>Settings Page</h1>
        </section>
    );
};

export default Settings;